[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "comms"
version = "0.1.0"
description = "A simple tool for communicating between 2 computers over the internet orlocally"
authors = [{ name = "Sheikh Shaheed Rijwan Wuhan", email = "ssrwuhan09@gmail.com" }]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.7"
dependencies = []
